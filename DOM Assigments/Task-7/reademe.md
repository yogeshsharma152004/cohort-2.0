# 🧩 DOM Task 7 – Instagram Like Feature

## 📌 Objective
Create an Instagram-like double-tap heart animation using DOM & JavaScript events.

## 🧠 What I Used & Learned
- `dblclick` event for double-tap effect
- `click` event to toggle like state manually
- DOM manipulation with `style` and `innerHTML`
- Animation logic using `transform` & `opacity`
- **classList methods**
  - `.add()`
  - `.remove()`
  - `.toggle()`
  - `.contains()`

## 🎨 UI Behavior
- Double-tap on the image → Heart pops up & animates
- Click on the heart icon → Like toggles ON/OFF
- Smooth show/hide animation for realistic effect

## 🚀 Outcome
This task helped me understand how **real social media features** are created —
interactive, animated, and responsive to user actions.

---
