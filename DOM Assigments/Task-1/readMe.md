## 🧮 DOM Task – 1: Counter App

### 📌 Overview
A basic counter application built using **DOM manipulation**.  
This task focuses on understanding how to interact with HTML elements using JavaScript.

---

### 🔹 Features
- **Increment** the counter  
- **Decrement** the counter  
- Real-time UI update using `innerText`
- Uses `addEventListener()` for button interactions

---

### 🔍 Concepts Used
- Selecting elements with `querySelector`
- Updating content using `innerText`
- Handling events with `addEventListener("click")`

---

### 💡 Takeaway
A simple but effective exercise to strengthen DOM fundamentals and event handling in JavaScript.

---
