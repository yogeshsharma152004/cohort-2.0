# DOM Task 5 – Random Quote Generator

This project generates random motivational quotes on the screen using JavaScript DOM manipulation.  
Each quote appears with dynamic styling like random colors, positions, rotation, and scaling.

## 📌 Features
- Displays a new quote on each button click  
- Positions the quote randomly on the screen  
- Random rotation for fun visual effect  
- Random RGB text color  
- Random scale (size) for variation  
- Stores quotes in an array and selects one at random  

## 🧠 Concepts Used
- `document.createElement()`
- DOM styling using JavaScript
- Random values with `Math.random()`
- Working with arrays
- Text manipulation and dynamic UI creation

---

This task strengthens DOM fundamentals and makes learning much more interactive and enjoyable.
