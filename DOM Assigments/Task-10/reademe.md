# 🎹 DOM Task 10 – Virtual Piano

## 📌 Overview
A fully functional Virtual Piano built using **HTML, CSS & JavaScript** where the user can play piano sounds using:
- Mouse Clicks
- Keyboard Keys 🎶

---

## 🔧 DOM Concepts Used
| Concept | Purpose |
|--------|--------|
| `querySelectorAll()` | Selecting multiple piano keys |
| `forEach()` | Looping through all keys |
| `click` event | Playing sound on button click |
| `keydown` event | Playing sound via keyboard |
| `classList.add()/remove()` | Key press animation |
| Custom `data-key` attribute | Mapping keyboard keys with audio |

---




