# DOM Task 4 – Random Colorful Box Generator

This task creates random colorful boxes on the screen using JavaScript and DOM manipulation.

## 📌 What It Does
- Generates a new box on every button click  
- Box appears at a **random position**  
- Has a **random rotation**  
- Background can be:
  - Solid RGB color  
  - Or a gradient color  

## 🧠 Key Concepts Used
- `document.createElement()`
- `appendChild()`
- `Math.random()`
- Inline styling through JS
- Absolute positioning in DOM


